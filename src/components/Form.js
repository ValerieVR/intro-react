import React from 'react';

const Form = () => {
    return (
        <div>
            <input type="text" placeholder="Type a new todo"/><br/>
            <button>Add Todo</button>
        </div>
    );
}

export default Form;